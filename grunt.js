//hello! I am a file used for Sass, please do not delete me :)
// -Kathleen

module.exports = function(grunt) {

const grunt = require("grunt")

grunt.initConfig({
    sass: {
      dist: {
        options: {
          loadPath: ['node_modules/foundation-sites/scss']
        }
      }
    }
  });

  )